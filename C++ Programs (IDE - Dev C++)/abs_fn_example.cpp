//Program to convert a negative no. to positive by using fn abs().(abs_fn_example) 
#include <iostream>
#include <math.h>

using namespace std;

int main()
{
	double x=-878,result;
	cout<<"x is :"<<x<<endl;
	result=abs(x);
	cout<<"abs("<<x<<") = |"<<x<<"| = "<<result;
	return 0;
	
}
